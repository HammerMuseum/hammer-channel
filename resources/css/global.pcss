.pinned {
  position: fixed;
  top: 0;
  z-index: 1000;
  left: 0;
  background: white;
  color: blue;
}

* {
  box-sizing: border-box;
}

html {
  -webkit-tap-highlight-color: transparent;
  -webkit-font-smoothing: antialiased;
}

html,
body,
div,
h1,
h2,
h3,
h4,
h5,
h6,
article,
aside,
footer,
header,
hgroup,
nav,
section {
  margin: 0;
  padding: 0;
  vertical-align: baseline;
}

body {
  font-family: var(--font-family--body);
  font-style: normal;
  font-weight: 300;
  color: var(--text-primary);
  font-size: var(--font-size--base);
  line-height: var(--line-height--base);
  background-color: var(--background-light);
}

[data-interaction-mode="pointer"] :focus {
  outline: none;
}

[data-interaction-mode="keyboard"] :focus {
  outline: var(--highlight-primary) auto 4px;
  outline-offset: -2px;
}

h1,
h2,
h3,
h4 {
  line-height: 1;
}

img {
  max-width: 100%;
  height: auto;
  width: auto;
}

ul,
li {
  list-style: none;
  padding: 0;
  margin: 0;
}

.hidden {
  display: none;
}

p {
  font-size: var(--font-size--base);
  line-height: var(--line-height--base);
  margin: 0 0 var(--line-height--base) 0;
}

.visually-hidden {
  position: absolute !important;
  height: 1px;
  width: 1px;
  overflow: hidden;
  clip: rect(1px, 1px, 1px, 1px);
  white-space: nowrap;
}

.carousels {
  padding-left: 10px;
  margin-bottom: 50vh;
}

li {
  margin-bottom: 8px;
}

.link--text {
  color: var(--text-primary);
  text-decoration: none;
  border-bottom: var(--primary-border-highlight);
}

.link--text:hover {
  color: var(--highlight-primary);
}

.link--text-and-button {
  display: inline-flex;
  align-items: center;
  border-bottom: none;
}

.link--text-and-button span {
  border-bottom: var(--primary-border-highlight);
}

.link--text-secondary {
  color: #fff;
}

.link--tag {
  display: inline-block;
  margin: 0 5px;
  font-weight: 700;
  color: #fff;
}

.heading {
  font-family: var(--font-family--heading);
  font-weight: 200;
}

.heading--primary {
  font-size: 48px;
}

.heading--secondary {
  font-size: 24px;
}

@media (min-width: 37.5em) {
  p {
    margin-bottom: 50px;
  }
}

@media (min-width: 52.5em) {
  h1,
  h2,
  h3,
  h4 {
    -webkit-font-smoothing: antialiased;
  }

  .heading {
    font-size: 60px;
  }

  .heading--secondary {
    font-size: 36px;
  }
}
