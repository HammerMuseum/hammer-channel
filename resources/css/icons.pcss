.icon {
  height: 36px;
  width: 36px;
  overflow: hidden;
  vertical-align: -0.15em;
  fill: var(--highlight-color-primary);
}

.icon--rotate {
  transform: rotate(180deg);
}

.icon--nav-bar-link {
  transform: rotate(270deg);
  height: 24px;
  width: 24px;
  margin-left: 8px;
}
